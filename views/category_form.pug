extends layout

block content
    h1= title 
    form(method='POST')
        div
        label(for='name') Category Name:
        input#name(type='text' name='category_type' value=(category === undefined ? '' : category.type))
        button(type='submit') #{title}